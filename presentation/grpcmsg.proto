syntax = "proto3";

option go_package = "./;grpcmsg";

package grpcmsg;

// 服務
service Grpcmsg {
  // Sends a greeting
  rpc SayHello (HelloRequest) returns (HelloReply) {}
}

// 請求消息
message HelloRequest {
  string name = 1;
}

// 響應消息
message HelloReply {
  string message = 1;
}
